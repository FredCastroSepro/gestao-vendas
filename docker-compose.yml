version: '3.8'

services:

   mysql:
      image: mysql:8.0.25
      container_name: mysql
      ports:
        - 3307:3306
      environment:
        MYSQL_USER: ugvendas
        MYSQL_PASSWORD: pgvendas  
        MYSQL_ROOT_PASSWORD: root
        MYSQL_DATABASE: gvendas_db